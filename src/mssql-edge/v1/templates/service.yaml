apiVersion: v1
kind: Service
metadata:
  name: {{ include "mssql-edge.fullname" . }}
  labels:
    {{- include "mssql-edge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mssql-edge.selectorLabels" . | nindent 4 }}
