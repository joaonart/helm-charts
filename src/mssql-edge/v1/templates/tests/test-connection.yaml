apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mssql-edge.fullname" . }}-test-connection"
  labels:
    {{- include "mssql-edge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mssql-edge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
