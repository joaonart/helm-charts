apiVersion: v1
kind: Service
metadata:
  name: {{ include "mssql-2019.fullname" . }}
  labels:
    {{- include "mssql-2019.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "mssql-2019.selectorLabels" . | nindent 4 }}
