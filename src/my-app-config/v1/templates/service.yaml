apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-config.fullname" . }}
  labels:
    {{- include "my-app-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-app-config.selectorLabels" . | nindent 4 }}
